export type DailyConfig = { [key: string]: any };
export type DailyGetterPromiseResponse = {
  [key: string]: { total: number; data: [] };
};
export type DailyResponseError = { [key: string]: string };
